-- Run this in pgAdmin or psql command line

-- Create the database
CREATE DATABASE greatcookie;

-- Connect to the database (in pgAdmin, you'll need to reconnect to 'greatcookie')
-- Then grant permissions to your user
-- If you're logged in as 'cokiee', you should already have access

-- Verify connection
SELECT current_database();
